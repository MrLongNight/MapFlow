## 2026-01-24 – Interactive Visualizers
**Learning:** Users expect visual representations of data (like timeline bars) to be directly manipulable, not just passive displays.
**Action:** Convert static `allocate_painter` regions into interactive widgets using `ui.interact` or transparent buttons on top of the canvas.
